'use strict';

var gulp        = require( 'gulp' );
var config      = require( '../config' );

gulp.task(
    'develop',
    function()
    {
    }
);

gulp.task(
    'watch_develop',
    function()
    {
    }
);
